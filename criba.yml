general:
  language: es
  timezone: America/Bogota
  heuristic_threshold: 0.6

ollama:
  host: http://host.docker.internal:11434
  model: qwen3.5:9b
  timeout: 30

sources:
  telegram:
    enabled: true
    poll_interval: 60
  reddit:
    enabled: true
    poll_interval: 120
  rss:
    enabled: true
    poll_interval: 300
  bluesky:
    enabled: true
    poll_interval: 120
  youtube:
    enabled: true
    poll_interval: 900
